# QORE Harness Engineer — CIBO Cognitive Expert R6 Correction-010

## PACKAGE

`HARNESS-ENGINEER-QORE-CIBO-COGNITIVE-EXPERT-R6-CORRECTION-010-ROOT-FAMILY-CLOSURE-001`

## ROLE

Act as the adversarial Harness Engineer for PR #486. This is an artifact-only causal-family correction package. Do not push, commit, merge, publish, authorize Production, use real capital, or absorb CIBO Functions / Trader Lab / Traders / Risk / provider authority.

This is NOT a restart of CIBO Cognitive and NOT a replay of Correction-009. Preserve all previously closed work. DeepSeek Expert R6 completed normally against the exact frozen Correction-009 candidate and returned `VALIDACIÓN NO OK` with exactly three IA-accepted material causal families. Coder is BLOCKED until this candidate mutates, passes FULL QG, is materialized, receives exact-head QORE CI, is frozen again, and passes a fresh Expert gate.

Correct exactly the three R6 material root families below plus the bounded neighboring equivalence classes required to establish Root-Family Exhaustion. Do not reopen R6 non-material observations or historical closures unless the new implementation creates a deterministic material contradiction.

## EXACT START BINDING

Repository: `mezas3238-hue/qore-core`
PR: `#486`
BASE: `9672c4d999bd5d3e6db544f349243bc6abea0363`
BASE TREE: `67c77fbe016b6688e5114165a5a14c3026832027`
START / HEAD: `62543e72331c43a14645eb33e53855900ecc7274`
START TREE: `f401ffffb479bda4aec98e6b490ae774b2ccafb6`
SYNTHETIC: `ab8fe510ee816bc9a4ed14cb81f6dbb997c04479`
Synthetic parents: BASE + START exactly
Synthetic TREE: `f401ffffb479bda4aec98e6b490ae774b2ccafb6`
Synthetic GitHub signature: verified / valid
Exact-head QORE CI: run `33919192997`, job `101173336463`
QG: Ruff PASS; mypy 775 files; pytest 5649/5649 PASS; 7 warnings; coverage 87%, 52,448 statements / 7,070 missed.

## EXPERT R6 EVIDENCE LINEAGE — EVIDENCE, NOT IMPLEMENTATION AUTHORITY

Package: `QORE-PR486-CIBO-COGNITIVE-FINAL-DS-EXPERT-R6-001`
Run: `33920209684`
Job: `101176563188`
Review ID: `5118026305`
Artifact: `9955274989`
Artifact digest: `sha256:196e856b9a60828130825cea21c59530fda519e0948bf6b3cdeb920d2ed70406`
Reviewer repository SHA at review: `e2bc2854be859a525b4074ff871dc84d82232b99`

R6 completed normally:
- all five Expert logical lanes completed;
- semantic LSP completed with usable definitions/references/hover/call-site evidence;
- durable reviewer checkpoints completed;
- adaptive HIGH/MAX reasoning enforcement passed;
- candidate remained immutable/read-only;
- exact frozen PR was revalidated immediately before publication;
- final semantic verdict: `VALIDACIÓN NO OK` with three material families.

Do not rerun or imitate R6. Independently reproduce each material witness on exact START, derive the causal invariant, explore neighboring space, implement the correction, and prove closure.

## PRESERVE VERIFIED / NON-MATERIAL AREAS

Treat these as regression obligations, not open investigations:
- Correction-009 exact causal-reference existence/fingerprint composition binding remains closed unless your new changes reopen it;
- typed causal mechanism/confounder-resolution architecture introduced by Correction-009 remains the baseline; R6 found a residual semantic constraint inside that contract, not a reason to rebuild causality;
- Correction-009 governed falsifier-resolution architecture remains baseline; R6 found residual CONFIRMED admission/cross-relabel gaps, not a reason to rebuild hypothesis lifecycle;
- R4 hypothesis channel-polarity typing and REFUTED genuinely-new-evidence gate;
- R3 Council firewall / no accidental authority;
- CapabilityEvidence retained-state integrity;
- canonical-instant identity/equality/dedup across equivalent offsets and DST-fold distinctions;
- exact runtime types, constructor/revalidation parity, nested corruption rejection;
- Council / `UNRESOLVED_CONTRADICTION` / NON_ACTIONABLE uncertainty coherence;
- deterministic replay/fingerprint/ordering and provider-neutral semantics.

R6 observations explicitly non-material unless the new patch makes one reachable/material:
- low-plausibility Basic branch-(c) trailing/internal-digit detection edge;
- direct-construction coverage freedom under the no-global-registry law;
- `revision_parent` local validation observation;
- `supersedes` pointer/status coupling observation;
- correlation→causation supersedes observation;
- broader cross-status/strength coupling observation;
- episode schema version not bumped because the serialized shape did not change and stale artifacts already fail closed by fingerprint.

# IA-ACCEPTED MATERIAL ROOT FAMILIES

## F1 — S-1 CREDENTIAL ASSIGNMENT SEMANTICS: FAIL-OPEN + GREEDY FALSE POSITIVES — HIGH

Primary location family: `src/qore/modules/cibo/cognitive_contracts.py`, especially credential-label tiers, `_COLON_PROSE_STOPWORDS`, `_COMPOUND_CRED_LABEL`, `_UNEQUIVOCAL_CRED_LABEL`, `_SECRET_PATTERNS`, `_is_detection_invisible`, secret skeleton/normalization, and all reachable Cognitive consumers of `contains_secret_material`.

### R6 deterministic witnesses

Fail-open real-credential assignments include:
- `password: one`
- `password: them`
- `client_secret: some`
- `api key: another`

The current stopword exemption can classify these as benign even though an unequivocal credential assignment must fail closed.

Greedy false-positive witnesses include benign/prose-like values such as:
- `access token: expires daily`
- `client id: unique`
- `openai key: billing`
- `personal access token: revoked`

Unicode-space neighboring behavior also remains semantically important: Zs collapse plus permissive `\s*` can join otherwise separate prose/labels, e.g. a Bearer phrase separated from neighboring material by NBSP. R6 confirmed ordinary `Bearer obligations 2024`, `Basic sha256`, and previously closed split-secret detection controls still behave as expected; do not regress them.

Reachability is material because secret validation flows through `_validate_safe_text`, `_validate_code`, `_validate_opaque_ref` and representative Cognitive carriers such as memory content, deliberation summaries, formal recommendation summaries, evaluation hypotheses, planning reflection notes and other call sites found through LSP.

### Root cause accepted by IA

Credential credibility discrimination remains witness/class-heuristic rather than class-sound:
- unequivocal credential assignments can be exempted solely because the value is an ordinary-language stopword;
- compound labels can reject ordinary prose solely because a label exists, without enough value-shape evidence;
- Unicode separator removal / regex whitespace tolerance can change token adjacency and semantic interpretation;
- fixing one witness by extending finite stopword tables merely moves the boundary.

### Required closure

Establish a principled deterministic two-sided detection contract:
1. UNEQUIVOCAL credential assignment labels must fail closed when syntactically used as credential assignments; ordinary words such as `one/them/some/another` cannot create an exemption merely because they are prose words.
2. Compound/ambiguous labels must not reject benign prose by label alone. Require a defensible credential-value signal (for example quoting, digit/entropy/credential-shaped syntax, known token structure, or another architecture-consistent structural discriminator) rather than a witness-specific word list.
3. Define and property-test Unicode separator/Cf/normalization behavior so normalization cannot silently concatenate semantically separate prose into a credential while still detecting intentionally split secret material.
4. Preserve previously closed AWS/GitHub/Slack/OpenAI/JWT/Bearer/Basic/private-key/URL-userinfo and split-secret families.
5. Preserve benign finance/security prose acceptance with explicit negative-control partitions.
6. Trace all `contains_secret_material` consumers with semantic LSP and prove no consumer-specific bypass.

Do NOT solve F1 by growing a stopword list, blanket Unicode rejection, blanket rejection of all credential-label prose, or one-off witness regexes. Close the root grammar/value-shape/normalization family.

## F2 — CONFIRMED HYPOTHESIS ADMISSION / CROSS-FALSIFIER RELABEL LAUNDERING — HIGH

Primary location: `src/qore/infrastructure/cibo_cognitive_hypotheses.py`, especially transition/build admission ordering, status/channel validation, `FalsifierResolution`, blocking-falsifier identity, logical values, fingerprints, retained history, serialization/replay and recursive revalidation.

### R6 deterministic witnesses

1. `transition_hypothesis(... -> CONFIRMED, contradictions=[new_falsifier])` can be accepted because the CONFIRMED path clears the supplied contradiction/against channels before those inputs are validated. The caller supplied blocking evidence therefore disappears rather than causing rejection.
2. Malformed values or wrong-polarity material supplied in new `evidence_against` / `contradictions` can likewise be discarded before validation.
3. With multiple blocking falsifiers, two falsifiers can be cross-relabeled as one another's resolving TEST_RESULT observation. The existing self-relabel guard is local to one resolution and can miss the global cross-resolution identity cycle, yielding CONFIRMED without genuinely new evidence.

R6 controls that must remain closed:
- partial resolutions rejected;
- direct self-relabel rejected;
- unrelated tests rejected;
- duplicate resolutions rejected;
- alternate-timezone representations deduplicate by canonical instant;
- REFUTED resurrection/new-evidence gate remains intact.

### Root cause accepted by IA

The confirmation path mutates/clears caller-supplied blocking channels before validating them, and falsifier freshness is enforced per resolution rather than globally over the blocking-falsifier identity set.

### Required closure

Preserve the law `HYPOTHESIS CONFIRMATION != FAVORABLE OUTCOME`:
1. Canonicalize, exact-type check, polarity-check and recursively revalidate all supplied evidence channels BEFORE any target-status projection/clearing.
2. CONFIRMED must reject newly supplied non-empty AGAINST or CONTRADICTION channels; it must never silently drop caller-supplied falsifying material.
3. Every historical blocking falsifier must retain its governed exact resolution.
4. A resolving test's canonical `(ref, UTC instant)` must be globally disjoint from the canonical `(ref, UTC instant)` of EVERY blocking falsifier, regardless of polarity or which resolution references it. Cross-relabeled falsifiers are not new evidence.
5. Preserve exact channel semantics, canonical-time equivalence, deterministic order/fingerprint/replay and constructor == transition == revalidate invariants.
6. Property/metamorphic tests must cover multi-falsifier permutations, cross-relabel cycles, wrong types, wrong polarity, malformed inputs, tuple order, timezone-equivalent instants, duplicate refs and reflective retained-state corruption.

Do NOT fix only the two-falsifier witness. Enforce the global identity-set invariant for arbitrary bounded collections.

## F3 — CAUSAL CONFOUNDER RESOLUTION EVIDENCE CAN BE CONTRADICTORY / UNRETAINED — MEDIUM-HIGH

Primary location: `src/qore/infrastructure/cibo_cognitive_causality.py`, especially `ConfounderResolution.revalidate`, canonical confounder-resolution handling, `CausalClaim._validate_coherence`, claim evidence channels, logical values/fingerprint, builders, integration/replay and recursive revalidation.

### R6 deterministic witness

A CONFIRMED CAUSATION claim can currently bind a `CausalEvidence` with CONTRADICTION polarity as the evidence that supposedly resolves a confounder. The resolution evidence can also be absent from the claim's own retained evidence channels. This lets a confirmed causal claim semantically depend on contradictory/unretained material while the coherence check sees no contradictory evidence in the claim channels.

R6 controls that must remain closed:
- exact confounder coverage-by-code;
- exact runtime types;
- canonical ordering/fingerprint parity;
- exact causal-reference composition binding;
- reflective corruption rejection;
- Correction-009 typed mechanism + typed confounder-resolution architecture.

### Root cause accepted by IA

`ConfounderResolution` currently validates structure and identity but does not constrain the semantic polarity or retained provenance of its evidence strongly enough. The claim-level coherence check only inspects claim evidence channels, so contradictory resolution evidence can sit outside that accounting boundary.

### Required closure

Preserve `CORRELATION != CAUSATION`:
1. Confounder-resolution evidence used to justify CAUSATION must have an explicitly admissible positive/supporting semantic polarity. AGAINST or CONTRADICTION evidence cannot count as confounder resolution; unsupported channel relabeling must fail closed.
2. Resolution evidence must be provenance-retained by exact canonical identity in the causal claim's governed evidence material. Prefer requiring exact identity membership in `evidence_for` if that is consistent with the existing contract; if a separate typed resolution-evidence channel is architecturally cleaner, it must still be retained, fingerprinted, replayable and included in claim coherence checks.
3. CONFIRMED cannot carry contradictory/against semantics indirectly through resolution objects.
4. Constructor, builder, recursive revalidation, logical values, fingerprint, integration and replay must enforce the same invariant.
5. Property/metamorphic tests must cover absent-from-channel evidence, SUPPORTS/AGAINST/CONTRADICTION/TEST_RESULT polarity partitions, duplicate/canonical-order permutations, equivalent-time identity, mismatched evidence identity and reflective corruption.

Do NOT weaken claim-level contradiction policy. Close the resolution-evidence semantic/provenance boundary.

# REQUIRED SIX LANES — EXACTLY 6/6

## L1 — Architecture / contracts / runtime / trust boundaries
- derive the two-sided credential assignment contract, global falsifier-resolution identity invariant, and causal resolution-evidence provenance/polarity contract;
- preserve CA-01..CA-18 ownership and `INTELLIGENCE != AUTHORITY`;
- map constructor, transition, retained-state, replay and integration trust boundaries.

## L2 — Witness reproduction / adversarial red-team
- reproduce every F1/F2/F3 R6 material witness on exact START before implementation;
- retain deterministic witness evidence;
- after implementation rerun exact witnesses plus neighboring partitions and explicitly report REPRODUCED-before / REJECTED-after.

## L3 — Security / Unicode / normalization / regex / parsing
- exhaust F1 credential label classes, unequivocal vs compound labels, quoted/unquoted values, prose verbs/stopwords, credential-shaped/digit/entropy classes, Zs/Cf/Mn and normalization order, known token families, URL userinfo and benign security/finance prose;
- maintain simultaneous false-negative and false-positive matrices;
- MAX reasoning mandatory for ambiguous security/Unicode closure.

## L4 — Property / metamorphic / systematic exploration
- F2 status × channel × blocking-falsifier × resolution × identity × canonical-time cross-product;
- F3 claim-status × polarity × retention × confounder-resolution × evidence identity cross-product;
- bounded arbitrary multi-falsifier sets, canonical ordering/permutation and equivalent-offset transforms;
- constructor == transition/build == revalidate == replay invariants.

## L5 — Historical regression / prior findings / neighboring family
- preserve R3/R4/Correction-008/Correction-009 closures listed above;
- inspect neighboring families only where new code has a concrete dependency/reachability relation;
- do not restart Cognitive history or broaden into R6 non-material observations without deterministic material evidence.

## L6 — Implementation impact / references / reachable paths / integration
- semantic LSP before and after: findReferences, goToDefinition, goToImplementation where applicable, hover, symbols/call sites and final impact recheck;
- trace `contains_secret_material` consumers;
- trace hypothesis build/transition/history/replay/integration;
- trace causal evidence -> confounder resolution -> causal claim -> hypothesis/integration -> replay/audit;
- final changed-path/diff audit and FULL QG.

Failure to provide usable evidence for all 6/6 lanes => BLOCKED.

# SEMANTIC LSP — MANDATORY

Provide concrete evidence of:
- `findReferences`;
- `goToDefinition`;
- `goToImplementation` when semantically applicable;
- `hover`;
- materially modified symbols;
- call sites / reachable consumers;
- final post-change recheck.

A textual claim without usable LSP evidence is insufficient.

# REASONING

HIGH baseline.

MAX mandatory for:
- F1 security / credential grammar / Unicode / normalization / regex ambiguity;
- F2 confirmation lifecycle, falsifier identity sets and cross-resolution contradictions;
- F3 causation, evidence provenance/polarity, trust-root and contradictory-evidence semantics;
- any architectural contradiction;
- final Root-Family Exhaustion adjudication.

Persist adaptive HIGH/MAX evidence in durable checkpoints.

# ROOT-FAMILY EXHAUSTION GATE

Do not stop when the listed witnesses pass. Explore bounded equivalence classes, neighboring transforms, cross-combinations, false positives/negatives, retained-state corruption, exact runtime types, recursive revalidation, constructor parity, replay/determinism and historical regressions. Use exhaustive enumeration when bounded and property/metamorphic generation otherwise.

Final semantic result must be exactly one of:
- `CANDIDATE READY — ROOT FAMILY EXHAUSTED`
- `BLOCKED / FURTHER MATERIAL FAMILY FOUND`

# FULL QUALITY GATE

Mandatory final candidate:
- `git diff --check`
- `ruff check .`
- `mypy src tests`
- `pytest --cov=src/qore --cov-report=term-missing`

No weakening tests, deleting tests, skip/xfail hiding, `type: ignore`, unjustified Ruff suppression, mypy relaxation, coverage exclusion or gate gaming.

# ARTIFACT-ONLY OUTPUT / CONTINUITY

No push/commit/merge to qore-core. Deliver:
- exact START/TREE verification;
- patch + patch SHA256;
- candidate tree/diff metadata;
- exact changed files;
- 6/6 lane evidence;
- semantic LSP evidence;
- HIGH/MAX reasoning evidence;
- witness/equivalence ledger;
- Root-Family Exhaustion closure argument;
- FULL QG output;
- durable checkpoints containing completed work, remaining obligations and exact next action.

If interrupted or the workflow turns red, recover the latest durable checkpoint/artifact first. Never redo completed lanes. A red workflow caused only by orchestration/terminal-marker parsing must not erase a complete candidate artifact.

No Claude. No Production. No real capital. No Risk authority. No execution authority. No Functions / Trader Lab / Trader implementation in this package.
