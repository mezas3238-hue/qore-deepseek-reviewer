# QORE TRADER LAB — FIRST COHORT WORK CLOSURE 001

## Authority and objective

This is a continuation/closure package over the exact ChatGPT Work candidate in qore-core PR #500. Do NOT restart the project, reimplement correct Work components, or broaden the immediate DEMO audit beyond VT-01, VT-08, VT-09, VT-17 and VT-31.

The mission is to finish the work contract, not to return scaffolding or a platform that still requires indispensable follow-up. The final Harness result must either be `CANDIDATE READY — FIRST COHORT DEEP RESEARCH CONTRACT CLOSED` or `BLOCKED / FURTHER MATERIAL FAMILY FOUND` with exact evidence. CI green alone is not closure.

## Exact qore-core candidate binding

- Repository: `mezas3238-hue/qore-core`
- PR: `#500` — MUST remain DRAFT
- Branch: `agent/qore-demo-first-execution-vertical-cert-001`
- BASE/main: `c98d486a760056d9a71173c7041cf6f0c58bd9e9`
- START/HEAD: `9b6139221895d473642092fe9c4c6ca64bd6235a`
- TREE: `d2ce1a3fcc6448c8cbbb77e2beee2e74cf9a3592`
- Work commit: `Complete first cohort deep research platform`
- QORE CI run 1988 / workflow run `34215139840`: SUCCESS on this exact HEAD.

Harness is artifact-only. Do not commit, push, merge, alter PR #500, or cancel/replace the active economic workflow. The economic workflow may continue independently while Harness audits/repairs the candidate in its disposable workspace.

## Scope — exact first DEMO cohort

- VT-01 NY Precision Core
- VT-08 CRT 4H AMD
- VT-09 Turtle Soup
- VT-17 QT Scalper
- VT-31 Silver Bullet

All five must be able to traverse the research battery. A failed qualification gate blocks promotion but MUST NOT terminate characterization/failure investigation. `FAILED GATE != END OF RESEARCH`.

Do not weaken gates, tune to pass, cherry-pick markets, reuse consumed OOS, or manufacture DEMO eligibility. `DEMO_ELIGIBLE = 0` is valid if the evidence says zero.

## Known audit state to preserve and falsify

Work materially improved the platform and appears to have closed earlier defects in:

- real economic horizon semantics: acquire with buffer and validate >=730 effective days for methodology-required M5/M15/H4;
- lifecycle separation including `ECONOMIC_EVIDENCE` before `DEMO_ELIGIBLE`;
- exact software SHA binding across evidence/aggregation;
- broader H4 linear-reference vs optimized bisect equivalence/property/metamorphic testing;
- deep characterization, failure analysis, hypothesis register and holdout governance;
- five-dossier and comparative-report generation;
- mechanical FULL QG.

Do NOT assume those are correct because Work or CI says so. Reproduce and independently falsify each causal family.

Material closure gaps observed by IA:

1. The real six-market execution on the new HEAD was not yet terminal at audit time, so final five dossiers/comparative results were not yet available.
2. The dossier path still represented `risk_review`, `cibo_review`, `independent_validation`, and `economic_evidence` as `not_supplied` rather than proving a complete integrated lifecycle where applicable.
3. Replay was represented as `completed_by_closed_bar_characterization`; prove semantic equivalence to the formal Replay gate or implement the actual gate.
4. The platform must distinguish qualification gates from research/diagnostic continuation: failure cannot erase the evidence needed to understand why the Trader failed.
5. Final promotion must be exact-version/config/methodology/software/evidence bound and fail closed on missing stages, weak samples, stale/consumed holdouts, mismatched market/account/software identity, or insufficient Monte Carlo applicability.

## NON-NEGOTIABLE SIX-SUBAGENT LAW

Harness MUST use EXACTLY SIX non-duplicative subagents/lanes. 6/6 completion evidence is mandatory.

If fewer than six lanes are actually used, if a lane is duplicated/empty, or if durable evidence cannot prove 6/6 completion, the package MUST return `BLOCKED` and MUST NOT emit candidate-ready status.

Required lanes:

### L1 — Architecture / lifecycle / exact contracts
Audit Trader Lab state machine, stage evidence, exact runtime types, promotion semantics, Replay/Fast-Forward/OOS/Stress/Monte-Carlo/Risk/CIBO/Independent/Economic ordering, terminal/version stickiness, and research-vs-promotion separation. Reproduce premature-promotion/bypass families and close them root-family-wide.

### L2 — Economic evidence / provenance / multi-market aggregation
Audit six-market evidence binding, actual >=730-day M5/M15/H4 span, acquisition buffer behavior, account/environment/software SHA binding, aggregation math, no mixed-head/mixed-account laundering, timeframe requirements, and data provenance. Property/adversarially test boundary spans (729d 23:59:59 vs >=730d), malformed ranges, missing timeframes and mixed identities.

### L3 — Deep characterization / failure investigation / dossiers
Audit VT-01, VT-08, VT-09, VT-17, VT-31 characterization completeness: decision/rejection funnel, abstention/setup/fill conversion, entry/SL/TP geometry, exits, MFE/MAE, drawdown/tails, side/session/regime/time/market behavior, parameter surface/stability, strengths/weaknesses, failure classes, limitations and exact blockers. Ensure failed Traders still generate complete research dossiers rather than disappearing after a gate.

### L4 — Replay / chronology / OOS / stress / Monte Carlo / holdout
Prove deterministic chronology, no lookahead/partial candle leakage, H4 reference==optimized equivalence, Replay semantic gate, Fast-Forward equivalence, untouched OOS selection discipline, stress evidence, Monte Carlo applicability/insufficient-evidence semantics, deterministic seed policy where used, and consumed-holdout -> fresh-holdout lineage for modified versions. Use property/metamorphic/generated cases, not witness-only tests.

### L5 — Risk / CIBO / independent validation / economic authority integration
Audit and, if missing, integrate the existing QORE authority path so survivors can authentically move through Risk Review -> CIBO Review -> Independent Validation -> Economic Evidence -> DEMO_ELIGIBLE without self-promotion or fabricated authority. A Trader blocked earlier must retain exact blocker and must not receive fake downstream PASS evidence. Verify exact evidence subject/version/config/SHA binding and no Risk/CIBO bypass.

### L6 — Integration / reachable paths / regression / closure
Trace all modified symbols, references, call sites, workflows/scripts, artifact producers/consumers, dossier/comparative generation and promotion entry points. Detect dead/scaffolding paths or parallel semantics. Close integration defects, run adversarial cross-component tests, perform final semantic revalidation, diff/blast-radius audit and canonical FULL QG.

## SEMANTIC LSP IS MANDATORY — NO LSP = BLOCKED

This package MUST NOT be accepted without semantic LSP evidence.

Grep/text search alone is insufficient.

Before material implementation, each lane must use semantic LSP on its owned symbols where applicable and record evidence. Required operations across the package:

- `goToDefinition`;
- `findReferences`;
- `hover` / exact type inspection;
- `goToImplementation` where an implementation relation exists;
- symbol/call-site discovery for every materially modified public or cross-module symbol.

After implementation, repeat semantic LSP on all materially modified symbols and their reachable call sites to prove integration and absence of stale alternate paths.

The final coordinator must provide an explicit LSP ledger mapping:

`lane -> symbol -> operation -> discovered definition/references/implementations/types/call-sites -> post-change recheck result`.

Fail-closed laws:

- missing LSP preflight -> `BLOCKED`;
- missing lane LSP evidence where semantically applicable -> `BLOCKED`;
- missing post-change LSP recheck -> `BLOCKED`;
- LSP unavailable/broken -> `BLOCKED`, not grep substitution;
- 6/6 lanes without LSP closure -> NOT candidate ready.

Use HIGH generally and MAX for lifecycle/authority contradictions, time/chronology/OOS leakage, economic-evidence binding, exact-type conflicts, replay equivalence, Risk/CIBO/Independent authority and final root-family closure.

## Required research semantics

For every Trader, the platform must be able to explain with retained evidence:

- what exact setup logic it used;
- when/why it operated or abstained;
- where fills did/did not occur;
- entry/SL/TP geometry and exit causes;
- expectancy/sample size/drawdown/tails;
- behavior by instrument, regime, session, side and time;
- parameter sensitivity/stability;
- IS vs OOS behavior;
- stress behavior;
- Monte Carlo behavior or explicit statistically justified `INSUFFICIENT_EVIDENCE`;
- exact gate failure(s);
- evidence-supported failure mechanisms versus mere correlations;
- falsifiable improvement hypotheses;
- consumed holdouts and required fresh unseen evidence.

No generic `bad performance` diagnosis is sufficient.

## Lifecycle target

The authoritative target is:

`RESEARCH -> REPLAY -> FAST_FORWARD -> OOS -> STRESS -> MONTE_CARLO -> RISK_REVIEW -> CIBO_REVIEW -> INDEPENDENT_VALIDATION -> ECONOMIC_EVIDENCE -> DEMO_ELIGIBLE`

Research/characterization may continue for a failed Trader where scientifically valid, but promotion must stop at the first failed mandatory gate.

## Economic evidence hard law

For every timeframe required by the exact methodology in this cohort:

- M5 actual span >= 730 days;
- M15 actual span >= 730 days;
- H4 actual span >= 730 days.

M1 is auxiliary unless the methodology itself requires it.

`requested lookback >=730` is not proof. Validate actual first/last evidence timestamps and fail closed below 730 days.

## H4 equivalence root family

Prove `linear_reference == optimized_version` with generated/property/metamorphic cases including empty history, first/last H4, exact close, just-before/just-after boundary, future exclusion, gaps, irregular spacing, long histories, contiguous tails, boundary windows and duplicate timestamp rejection.

## Holdout governance

If OOS evidence is read to formulate/change a methodology/configuration, that holdout is consumed for the successor version. Preserve lineage:

`parent version -> evidence -> hypothesis -> change -> new version -> fresh unseen holdout`.

No changed version may reuse consumed OOS as independent validation.

## Five final dossiers and cohort report

The system must be able to produce one final deep-characterization dossier for each of the five Traders and only after all five individual dossiers exist produce the first-cohort comparative report.

Artifacts/dossiers must be exact-SHA/account/environment/methodology/config bound. Do not fabricate missing external evidence. If a real external prerequisite is absent, represent `BLOCKED_EXTERNAL` precisely; internal missing wiring/tests are engineering work and must be fixed.

## Testing requirements

Use normal, negative, boundary, adversarial, generated, property and metamorphic tests. Do not weaken tests, thresholds, strict typing, coverage, authority, OOS or economic requirements.

Forbidden:

- `type: ignore` to conceal defects;
- unjustified `noqa`;
- skip/xfail to hide failures;
- deleting/weaking tests;
- reducing mypy strictness;
- coverage gaming;
- cherry-picking results;
- changing thresholds after seeing outcomes;
- seed hunting;
- OOS leakage/reuse;
- fake stage evidence;
- fake DEMO eligibility.

## FULL QG — mandatory after all 6 lanes and LSP closure

Run exactly:

- `ruff check .`
- `mypy src tests`
- `pytest --cov=src/qore --cov-report=term-missing`
- `git diff --check`

FULL QG before 6/6 lanes + LSP closure is not sufficient for candidate-ready status.

## Artifact-only delivery

Do not push/merge qore-core. Deliver:

1. exact START/TREE binding;
2. 6/6 lane ledger and durable checkpoints;
3. complete semantic LSP ledger before/after;
4. findings with root causes/causal families;
5. semantic patch and changed-files manifest;
6. focused/adversarial/property/metamorphic evidence;
7. FULL QG logs/coverage;
8. exact closure argument;
9. remaining external blockers, if any;
10. safe materialization instructions for the Integration Authority.

Candidate patch must exclude coverage databases, recovery scratch files, credentials/secrets and unrelated repository material.

## Candidate-ready law

Harness may emit:

`CANDIDATE READY — FIRST COHORT DEEP RESEARCH CONTRACT CLOSED`

ONLY if all of the following hold simultaneously:

- exact 6/6 non-duplicative subagents completed;
- mandatory semantic LSP pre/post evidence is complete;
- known IA findings were reproduced and root-family closed or explicitly falsified with evidence;
- first-cohort research platform is fully integrated rather than scaffolded;
- failed Traders remain deeply characterizable;
- lifecycle/promotion authority is fail-closed;
- economic evidence semantics enforce >=730 actual days;
- SHA/account/config/methodology bindings are exact;
- Replay/OOS/stress/Monte Carlo/holdout semantics are scientifically coherent;
- Risk/CIBO/Independent/Economic path is genuinely wired for surviving candidates;
- normal/adversarial/property/metamorphic tests pass;
- FULL QG passes;
- no indispensable internal TODO/follow-up remains.

Otherwise return:

`BLOCKED / FURTHER MATERIAL FAMILY FOUND`

with exact evidence and retained checkpoint. No Production/LIVE/real-capital authority is introduced by this package.
