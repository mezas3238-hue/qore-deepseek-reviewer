# QORE TRADER LAB — FIRST COHORT WORK CLOSURE 002

## Successor authority

This is the mandatory successor to `harness/engineer/tasks/qore-trader-lab-first-cohort-work-closure-001.md` after Harness run `34218989025` failed before producing valid durable 6/6 evidence.

Read the predecessor task first. EVERY substantive requirement, scope boundary, six-lane responsibility, semantic-LSP requirement, lifecycle rule, economic-evidence rule, holdout rule, FULL QG rule and candidate-ready law in closure-001 remains binding here and is inherited verbatim unless this successor explicitly tightens it.

## Exact qore-core binding — unchanged

- Repository: `mezas3238-hue/qore-core`
- PR: `#500` — MUST remain DRAFT
- Branch: `agent/qore-demo-first-execution-vertical-cert-001`
- START/HEAD: `9b6139221895d473642092fe9c4c6ca64bd6235a`
- TREE: `d2ce1a3fcc6448c8cbbb77e2beee2e74cf9a3592`
- Artifact-only: no commit, push, merge, PR mutation, workflow mutation, credential mutation or LIVE/Production authority.

## Predecessor failure and recovery evidence

Predecessor Harness run: `34218989025`.

The predecessor did NOT certify 6/6. Its durable journal remained at 0/6 and therefore its textual statement `All six lanes are complete` is NOT admissible completion evidence.

However the failed run produced a recovery candidate patch which the host will pre-apply only after exact SHA-256 verification:

- artifact id: `10054157398`
- candidate patch SHA-256: `98d1cf8ca09c789295e49ecc326ca5b862b72af01cb96c6f14e5d3bf933b1411`

Treat that recovered patch as UNTRUSTED WORK-IN-PROGRESS. It may save implementation time, but it grants no lane completion, no semantic correctness, no LSP credit, no QG credit and no candidate-ready status. Re-audit it using all six required lanes and semantic LSP.

## Root cause of predecessor failure

The predecessor emitted a durable lane state `IN_FLIGHT`. The host state machine intentionally accepts only its canonical fail-closed vocabulary and rejected that unknown state. Because the invalid checkpoint was never committed to the durable journal, 6/6 could not be proven.

This successor therefore tightens the checkpoint protocol.

## CANONICAL DURABLE STATE VOCABULARY — EXACT

For BOTH `QORE_LANE_STATE` and `QORE_SUBAGENT_STATE`, the ONLY permitted state values are:

- `NOT_STARTED`
- `DISPATCHING`
- `RUNNING`
- `CHECKPOINTED`
- `COMPLETED`
- `RECOVERY_REQUIRED`
- `MATERIAL_BLOCKED`

`IN_FLIGHT` IS FORBIDDEN. Do not invent synonyms such as `DONE`, `ACTIVE`, `WORKING`, `IN_PROGRESS`, `PASS`, `FINISHED`, or any other state token.

If work is currently executing, use `RUNNING`.

If usable partial work has been durably preserved but the lane is not complete, use `CHECKPOINTED`.

If interrupted work must be resumed, use `RECOVERY_REQUIRED`.

## DURABLE CHECKPOINT DISCIPLINE — FAIL CLOSED

Do not wait until global synthesis to record lane progress.

Before substantive lane work:

1. assign exactly six distinct non-placeholder subagent identities, one per lane;
2. durably record each lane/subagent as `DISPATCHING` or `RUNNING` using the canonical vocabulary;
3. preserve exact package/START/TREE binding in every checkpoint block.

For EACH lane independently:

- write a complete durable checkpoint as soon as the lane reaches a reusable intermediate state;
- write another complete durable checkpoint immediately when that lane and its exact subagent reach `COMPLETED`;
- include the lane's semantic-LSP evidence ledger or a precise pointer to durable evidence generated by that lane;
- never mark a lane `COMPLETED` until its owned investigation, adversarial/property work, implementation impact analysis, and required LSP pre/post checks are complete.

A generation timeout must not be able to erase evidence for already completed lanes.

Do NOT claim `all six lanes complete` unless the host-readable durable journal itself proves:

- lanes 1..6 are `COMPLETED`;
- six distinct non-placeholder subagent IDs are bound one-to-one to lanes 1..6;
- subagent states for lanes 1..6 are `COMPLETED`;
- no completed lane/subagent regressed;
- required LSP evidence exists for all semantically applicable work.

## SIX-SUBAGENT LAW — RE-CERTIFICATION REQUIRED

Exactly six non-duplicative subagents are still mandatory:

- L1 Architecture / lifecycle / exact contracts
- L2 Economic evidence / provenance / multi-market aggregation
- L3 Deep characterization / failure investigation / dossiers
- L4 Replay / chronology / OOS / stress / Monte Carlo / holdout
- L5 Risk / CIBO / independent validation / economic authority integration
- L6 Integration / reachable paths / regression / closure

The recovered patch does not satisfy any lane automatically. Every lane must independently inspect the recovered candidate and either accept, repair or reject its relevant changes with evidence.

Fewer than 6/6 or missing durable identity/state evidence => `BLOCKED`.

## SEMANTIC LSP — STILL ABSOLUTELY MANDATORY

`NO LSP = BLOCKED` remains unchanged.

The predecessor host LSP smoke succeeded, but that smoke test is infrastructure proof only and does NOT substitute for lane-level semantic LSP evidence.

Each applicable lane must actually use and durably record:

- `goToDefinition`
- `findReferences`
- `hover` / exact type inspection
- `goToImplementation` where applicable
- call-site/symbol discovery for materially modified public/cross-module symbols
- post-change semantic recheck on those symbols and reachable call sites

Grep/text search is supplemental only and cannot replace LSP.

Missing pre-change LSP, missing lane LSP, missing post-change LSP, broken LSP, or fabricated LSP evidence => `BLOCKED`.

## Recovery-candidate audit priorities

The recovered patch currently touches at least the first-cohort research dossier implementation and tests. Do not assume that scope is sufficient or correct. Falsify it against the full inherited closure-001 contract, especially:

- research dossier versus governed lifecycle authority separation;
- exact Risk/CIBO/Independent/Economic stage truthfulness;
- >=730 effective-day evidence based on real timestamps, not requested lookback;
- exact account/environment/software/methodology/config bindings;
- failed-Trader characterization continuation;
- Replay semantics and chronology;
- OOS/holdout consumption and fresh holdout requirements;
- Monte Carlo applicability/insufficient evidence semantics;
- no fabricated downstream PASS evidence;
- no self-promotion to DEMO_ELIGIBLE;
- integration and call-site consistency across all reachable producers/consumers.

## Time and resilience discipline

The predecessor consumed one 2100-second Engineer generation and timed out before valid durable closure. This successor must optimize for durable progress rather than one monolithic final response.

- checkpoint early;
- checkpoint lane-by-lane;
- preserve recovered useful work;
- do not redo completed durable work within this successor;
- perform global synthesis only after host-readable 6/6 completion exists;
- leave enough execution budget for Internal Expert audit/repair and canonical FULL QG.

## Completion gates

Candidate-ready remains impossible unless all inherited closure-001 gates pass, including:

1. exact START/TREE binding;
2. 6/6 distinct non-duplicative subagents durably complete;
3. complete lane-level semantic LSP pre/post ledger;
4. recovered patch independently falsified/repaired rather than trusted;
5. root-family closure for all material findings;
6. canonical FULL QG:
   - `ruff check .`
   - `mypy src tests`
   - `pytest --cov=src/qore --cov-report=term-missing`
   - `git diff --check`
7. independent Internal Expert CLEAN after final candidate mutation;
8. no indispensable internal follow-up;
9. no fake DEMO eligibility or external authority.

Only then may Harness emit:

`CANDIDATE READY — FIRST COHORT DEEP RESEARCH CONTRACT CLOSED`

Otherwise it must emit:

`BLOCKED / FURTHER MATERIAL FAMILY FOUND`

with the exact durable state, causal reason and recovery evidence.